(function(){    // ファイル選択ダイアログを表示する    var srcFolderObj = Folder.selectDialog("コピー元のテキストファイルがあるフォルダを選択してください");    if (!srcFolderObj){ return; }    // キャンセルされた場合は何も処理しない    // ファイル選択ダイアログを表示する    var dstFolderObj = Folder.selectDialog("コピー先のフォルダを選択してください");    if (!dstFolderObj){ return; }    // キャンセルされた場合は何も処理しない    // テキストファイル一覧を取得する    var fileList = srcFolderObj.getFiles("*.txt");    for(var i=0; i<fileList.length; i++){        var copyFileName = dstFolderObj+"/"+fileList[i].name;        var flag = fileList[i].copy(copyFileName);        if (flag == false){ // コピーできなかった            $.writlen("失敗："+copyFileName)        }    }    alert("終了しました");})();