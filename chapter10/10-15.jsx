(function(){    // ファイル選択ダイアログを表示する    var savefileObj = File.saveDialog("保存ファイル名を入力してください");    if (!savefileObj){ return; }    // キャンセルされた場合は何も処理しない    var flag = savefileObj.open("w");   // 書き込みモードで開く    if (!flag){        alert("ファイルに書き込めません");        return;    }    // バイナリ形式を指定する    savefileObj.encoding = "binary";    // バイナリデータを入れる変数    var data = "";    // ファイルにデータを書き込む    for(var y=0; y<256; y++){   // 縦256ピクセル        for(var x=0; x<256; x++){   // 横256ピクセル            var r = String.fromCharCode(255);   // 赤色の輝度            var g = String.fromCharCode(x & 255);   // 緑色の輝度            var b = String.fromCharCode(y & 255);   // 青色の輝度            data = data + r+g+b;        }    }    // 作成したバイナリデータを保存する    savefileObj.write(data);    savefileObj.close();    alert("書き込み完了");})();