(function(){    // ==============================================    // 定期的に時間を出力する関数    // ==============================================    function displayTime(){        // 現在の日付と時間を出力する        $.writeln(new Date());    }    var oldSec = -1;    // ポーリングする    while(true){        // 現在の秒数を取得する        var dateObj = new Date();        var sec = dateObj.getSeconds();        // 以前の秒数と違う場合は関数を呼び出す        if (sec != oldSec){            displayTime();            oldSec = sec;   // 古い秒数を保存する変数に現在の秒数を入れる        }    }})();