(function(){    // ファイル選択ダイアログを表示する    var savefileObj = File.saveDialog("保存ファイル名を入力してください");    if (!savefileObj){ return; }    // キャンセルされた場合は何も処理しない    var flag = savefileObj.open("w");   // 書き込みモードで開く    if (!flag){        alert("ファイルに書き込めません");        return;    }    // 文字コードを指定する    savefileObj.encoding = "utf-8";    // ファイルにテキストを書き込む    savefileObj.writeln("Adobeアプリ自動化");    savefileObj.writeln("プロジェクト");    savefileObj.close();})();