function setOpacity(data){    try{        if(activeDocument.layers[data.layerName]){            activeDocument.layers[data.layerName].opacity = data.opacity;            return true;        }    }catch(e){        return false;    }}setOpacity({ layerName : "樹木", opacity : 50 });setOpacity({ layerName : "レイヤー 1", opacity : 80 });setOpacity({ layerName : "テロップ", opacity : 75 });