// ファイル保存ダイアログを表示するvar fileObj = File.saveDialog("保存先を指定してください");if (fileObj != null){  // ファイル名が指定された場合に処理する    $.writeln("保存するファイル名は「"+fileObj.name+"」です。");    // ファイルに書き込めるかどうか確認する    var flag = fileObj.open("w");    if (flag == true){  // 書き込める場合に処理する        $.write("書き込み可能です");        fileObj.write("書き込みました");        fileObj.close();    }else{        $.writeln("書き込めません");    }}else{    $.writeln("キャンセルされました");}