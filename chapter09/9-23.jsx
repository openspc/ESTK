var text = "ABCDEFGHIJK";var chr = text.charAt(5);   // 6番目の文字を抜き出す$.writeln(chr);var str = text.substring(2, 6); // 3番目から6文字目までを抜き出す$.writeln(str);str = text.substr(2, 6);    // 3番目から6文字抜き出す$.writeln(str);str = text.slice(-3);   // 右側から3文字抜き出す$.writeln(str);str = text.slice(3);   // 3文字目から最後まで抜き出す$.writeln(str);str = text.slice(1, -1);    // 両側の1文字を削除$.writeln(str);