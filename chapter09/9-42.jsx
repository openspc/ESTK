// オブジェクトを生成するための設計図function customLayer(name, type){    // プロパティに値を設定    this.layerName = name;    // プロパティに値を設定    this.layerType = type;    // メソッドを定義    this.getName = function(){ return "レイヤー名は"+this.layerName+"です"; };    this.getType = function(){ return "種類は"+this.layerType+"です"};}// オブジェクトを生成var myLay = new customLayer("センターレイヤー", "text");// 各プロパティの内容を表示する$.writeln(myLay.layerName);$.writeln(myLay.layerType);$.writeln("------------------");// プロパティを削除delete myLay.layerType;$.writeln(myLay.layerName);$.writeln(myLay.layerType);$.writeln("------------------");var name = myLay.getName();$.writeln(name);// メソッドを削除delete myLay.getName;try{    var name = myLay.getName(); // メソッドがないのでエラーになる}catch(e){    name = e;}$.writeln(name);