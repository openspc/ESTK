// 再帰処理を行う関数1function nest(level){    if (level == 0){ return; }  // レベル0なら以後の処理はしない    $.writeln("Level = "+level);    level = level - 1;    nest(level);   // 自分自身を呼び出す}nest(4);$.writeln("-----------");// 無名関数で再帰処理を行う場合(function(level){    if (level == 0){ return; }  // レベル0なら以後の処理はしない    $.writeln("Level = "+level);    level = level - 1;    arguments.callee(level);   // 自分自身を呼び出す})(6);