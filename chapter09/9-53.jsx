// フォルダ選択ダイアログを表示するvar folderObj = Folder.selectDialog("フォルダを選択してください");if (folderObj != null){  // 何か選択された場合に処理する    var fileList = folderObj.getFiles("*");    // フォルダのみ表示する    for(var i=0; i<fileList.length; i++){        if (fileList[i].getFiles){  // フォルダの場合はgetFilesメソッドがある            $.writeln(decodeURI(fileList[i].name));        }    }}else{    $.writeln("キャンセルされました");}