// フォルダ選択ダイアログを表示するvar folderObj = Folder.selectDialog("フォルダを選択してください");if (folderObj != null){  // フォルダが選択された場合に処理する    // 選択されたフォルダのパスを取得する    var fPath = folderObj.fsName;    // 新たに作成するフォルダオブジェクトを生成する    var psdFolder = new Folder(fPath + "/psd");    // フォルダを作成する    psdFolder.create();    $.writeln(fPath + "/psd"+"にフォルダを作成しました");}else{    $.writeln("キャンセルされました");}