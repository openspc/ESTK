// オブジェクトを生成するための設計図function customLayer(name, type){    // プロパティに値を設定    this.layerName = name;    // プロパティに値を設定    this.layerType = type;}// オブジェクトを生成var myLay1 = new customLayer("マットレイヤー", "text");var myLay2 = new customLayer("ドラゴンスレイヤー", "game");// 結果を表示する$.writeln(myLay1.layerName);$.writeln(myLay1.layerType);$.writeln(myLay2.layerName);$.writeln(myLay2.layerType);$.writeln("------------------");Math.perfectNumber = 496;$.writeln(Math.perfectNumber );