// オブジェクトを生成するための設計図function countParagaraph(filename){    // ファイル名    this.name = filename;    // ファイルオブジェクトを自動生成    this.fileObj = new File("~/"+filename);    this.count = function(){        var flag = this.fileObj.open("r");        if (flag == true){            var count = 0;            while(!this.fileObj.eof){                this.fileObj.readln();                count = count + 1;            }            this.fileObj.close();        }else{            count = -1;        }        return count;    }}// オブジェクトを生成var file1 = new countParagaraph("index.html");// 定義したオブジェクトのメソッドを呼び出すvar n = file1.count();// 結果を表示する$.writeln(n);var file2 = new countParagaraph("test.txt");n = file2.count();$.writeln(n);