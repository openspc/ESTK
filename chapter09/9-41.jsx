// オブジェクトを生成するための設計図function customLayer(name, type){    // プロパティに値を設定    this.layerName = name;    // プロパティに値を設定    this.layerType = type;    // メソッドを定義    this.setLayerType = function(type){        // 新しいレイヤーの種類を設定        this.layerType = type;    }}// オブジェクトを生成var myLay = new customLayer("アヤナミレイヤー", "character");// レイヤーの種類を表示する$.writeln(myLay.layerType);// 新しい種類を設定するmyLay.setLayerType("anime");$.writeln(myLay.layerType);