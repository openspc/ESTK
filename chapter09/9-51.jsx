// ファイル選択ダイアログを表示するvar fileObj = File.openDialog("ファイルを選択してください");if (fileObj != null){  // 何か選択された場合に処理する    // ファイルが開けるか確認する    var flag = fileObj.open("r");    if (flag == true){        // 行数をカウントする変数を用意する        var count = 0;        // ファイルの終わりまで読み込む        while(!fileObj.eof){            var text = fileObj.readln();    // 1行読み込む            count = count + 1;        }        fileObj.close();    // ファイルを閉じる        $.writeln(count+"行ありました");    }}else{    $.writeln("キャンセルされました");}